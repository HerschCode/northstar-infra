# root: dev
# expect: KEY-001
# what: a JSON key for a quick local test, which then lives in Terraform state forever.
resource "google_service_account_key" "mutation" {
  service_account_id = "projects/${var.project_id}/serviceAccounts/gateway-sa@${var.project_id}.iam.gserviceaccount.com"
}
